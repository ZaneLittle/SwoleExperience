apiVersion: v1
kind: Service
metadata:
  name: swole-experience-react
spec:
  selector:
    app: swole-experience-react
  ports:
    - name: http
      port: 42069
      targetPort: 42069
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swole-experience-react
  labels:
    app: swole-experience-react
spec:
  replicas: 1
  selector:
    matchLabels:
      app: swole-experience-react
  template:
    metadata:
      labels:
        app: swole-experience-react
    spec:
      containers:
      - name: swole-experience-react
        image: swole-experience-react:latest
        ports:
        - containerPort: 42069
        env:
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 42069
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 42069
          initialDelaySeconds: 5
          periodSeconds: 5


